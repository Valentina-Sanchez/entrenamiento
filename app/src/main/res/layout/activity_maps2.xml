<?xml version="1.0" encoding="utf-8"?>

<LinearLayout android:layout_height="match_parent"
    android:layout_width="match_parent"
     xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hibrido"
        android:textColor="@color/primary_text"
        android:theme="@style/BtnBooking"
        android:id="@+id/tp1"
        android:onClick="onCambiarHibrido"></Button>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:theme="@style/BtnBooking"
        android:textColor="@color/primary_text"
        android:text="Normal"
        android:id="@+id/tp2"
        android:onClick="onCambiarNormal"></Button>
    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:theme="@style/BtnBooking"
        android:textColor="@color/primary_text"
        android:text="Satelital"
        android:id="@+id/tp3"
        android:onClick="onCambiarSatelital"></Button>
        <Button
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:theme="@style/BtnBooking"
            android:textColor="@color/primary_text"
            android:text="Terreno"
            android:onClick="onCambiarTerreno"
            android:id="@+id/tp4"></Button>
    </LinearLayout>

    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:map="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".Maps2" ></fragment>



</LinearLayout>
